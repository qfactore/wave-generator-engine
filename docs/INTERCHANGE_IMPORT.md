# Interchange Import

The Interchange is discovered by explicit path, environment override, or exact
sibling path. Required paths come from its handoff manifest. All required JSON
is parsed, all declared schemas are compiled, and WG-I8 policies are validated.
External paths are never copied into readiness reports.
