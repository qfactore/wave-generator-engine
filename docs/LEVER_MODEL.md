# Lever Model

Basic and Advanced are two views over one future LeverSet, not separate engines
or formats. Basic will expose concise validated controls. Advanced will expose
bounded detail. Exact profiles lock disallowed controls. Pulse Pattern internals
remain hidden in Basic. WGE-0 has no functional levers.
