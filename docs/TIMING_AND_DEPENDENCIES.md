# Timing and Dependencies

Exact motif-internal timing is immutable. Exact mode exposes no carrier-frequency
control, and schedule-spectrum peaks are not carrier values. Pulse Pattern is
mode data and does not time-scale motifs. Complex Mode requires macro states
before packets and events. Focus Role moves with density emphasis while global
playback intensity and calibration remain separate from physical mapping.
