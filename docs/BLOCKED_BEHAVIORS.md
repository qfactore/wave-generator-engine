# Blocked Behaviors

The typed gate registry is assembled from handoff blocks, decision-registry
alternatives, WG-I8 artifact blocks, and engine scaffold gates. Every known
unsafe request is rejected. Unknown requests also fail closed. Production and
diagnostic output requests are blocked because WGE-0 authorizes no exporter.
